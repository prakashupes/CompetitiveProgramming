#include<iostream>
#include<string.h>
using namespace std;
int main()
{
/*
    string s="hacker";
    string shifted;
    int n=s.length();
    int k=3;
    for(int i=0;i<s.length();i++)
    {
        int index=(i+k)%n;
        shifted.at(index)=s[i];
    }
    for(int i=0;i<s.length();i++)
    {
        cout<<shifted;
    }
    */

    string str="hello";
    char str2[str.size()];





}
